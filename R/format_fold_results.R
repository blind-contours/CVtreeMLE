
format_fold_results <- function(marginal_rules,
                                mix_rules,
                                mix_combo_data,
                                marg_combo_data) {








  return(list(
    "marginal_rules" = marginal_rules,
    "mix_rules" = mix_rules
  ))
}
